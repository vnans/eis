{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
<main id="main" class="main-page">

  <!--==========================
    Speaker Details Section
  ============================-->
  <section id="speakers-details" class="wow fadeIn">
    <div class="container">
      <div class="section-header">
        <h2> Détails du produit {{ produit.libelle}}</h2>
        <p></p>
      </div>

      <div class="row">
        <div class="col-md-6">
          <img src="/img/{{produit.image}}" alt="{{produit.image}}" class="img-fluid">
        </div>

        <div class="col-md-6">
          <div class="details">
            <h2>{{produit.libelle}}</h2>
            <div class="social">
              <a href=""><i class="fa fa-twitter"></i></a>
              <a href=""><i class="fa fa-facebook"></i></a>
              <a href=""><i class="fa fa-google-plus"></i></a>
              <a href=""><i class="fa fa-linkedin"></i></a>
            </div>
            <p>{{ produit.detail}}</p>

          </div>
        </div>

      </div>
    </div>

  </section>

</main>


    <!-- <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ produit.libelle }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ produit.detail }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit.image }}</td>
            </tr>
            <tr>
                <th>Piecejointe</th>
                <td>{{ produit.piecejointe }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ produit.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">back to list</a>

    <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit/_delete_form.html.twig') }} -->
{% endblock %}
