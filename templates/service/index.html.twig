{% extends 'base.html.twig' %}

{% block title %}Service index{% endblock %}

{% block body %}
    <main id="main" >
      <!--==========================
        Speakers Section
      ============================-->
      <section id="speakers" class="wow fadeInUp">
        <div class="container">
          <div class="section-header">
            <h2>NOS SERVICES</h2>
            <!-- <p> <a href="{{path('service_index') }}" class="buy-tickets"> <button class="btn btn-success" type="button" name="button">En savoir plus</button> </a>  </p> -->
          </div>

          <div class="row">
          {% for service in services %}
          <div class="col-lg-4 col-md-6">
            <div class="speaker">
              <img src="/img/{{ service.image }}" alt="/img/{{ service.image }}" class="img-fluid">
              <div class="details">
                <h3><a href="{{ path('service_show', {'id': service.id}) }}">{{ service.libelle }}</a></h3>
                <p><a href="{{ path('service_show', {'id': service.id}) }}">Details</a> </p>
                <div class="social">
                  <a href=""><i class="fa fa-twitter"></i></a>
                  <a href=""><i class="fa fa-facebook"></i></a>
                  <a href=""><i class="fa fa-google-plus"></i></a>
                  <a href=""><i class="fa fa-linkedin"></i></a>
                </div>
              </div>
            </div>
          </div>

          {% else %}
              <tr>
                  <td colspan="7">Rien trouvé </td>
              </tr>
          {% endfor %}
        </div>
        </div>

      </section>


    </main>

    <!-- <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Detail</th>
                <th>Image</th>
                <th>Piecejointe</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ service.id }}</td>
                <td>{{ service.libelle }}</td>
                <td>{{ service.detail }}</td>
                <td>{{ service.image }}</td>
                <td>{{ service.piecejointe }}</td>
                <td>{{ service.slug }}</td>
                <td>
                    <a href="{{ path('service_show', {'id': service.id}) }}">Details</a>
                    <a href="{{ path('service_edit', {'id': service.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('service_new') }}">Nouveau</a> -->
{% endblock %}
